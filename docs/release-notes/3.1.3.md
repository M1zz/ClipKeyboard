# 3.1.3 릴리즈 노트

앱 스토어 "이번 버전의 새로운 기능"에 그대로 붙여 넣을 수 있는 문안입니다.
(앱 안 변경 이력은 `ChangelogView.swift` 의 `3.1.3` 항목, 저장소 기록은 `HISTORY.md`)

아래 두 문안은 **특수기호를 쓰지 않은 평문**입니다. App Store Connect 의 입력칸은
마크다운을 해석하지 않아서, 별표나 가운뎃점을 넣으면 그 글자가 그대로 보입니다.
그대로 복사해 붙이면 됩니다.

---

## 한국어 (App Store)

```
키보드 오른쪽 끝이 잘려 보이던 것을 고쳤습니다.

자주 쓰는 것을 즐겨찾기에 두면 어떤 점이 좋은지, 목록에서 한 번 짚어 드립니다.
```

---

## English (App Store)

```
Fixed the right edge of the keyboard being clipped.

The list now points out, once, what favoriting a snippet does for you.
```

---

## 심사·배포 메모 (내부)

⚠️ **이 문서는 나중에 기록에서 되살린 것이다.** 그때 실제로 스토어에 올린 문안이 아니라,
버전을 올린 커밋과 그 앞의 작업 기록을 훑어 **그 버전에서 바뀐 것**을 다시 쓴 것이다.
당시 문안이 따로 있었다면 그쪽이 사실이다.

- 버전 올린 커밋: `ec7d81b` (2026-02-18)
- 같은 날 3.1.2 다음에 바로 올린 작은 수정판이다
