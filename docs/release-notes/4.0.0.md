# 4.0.0 릴리즈 노트

앱 스토어 "이번 버전의 새로운 기능"에 그대로 붙여 넣을 수 있는 문안입니다.
(앱 안 변경 이력은 `ChangelogView.swift` 의 `4.0.0` 항목, 저장소 기록은 `HISTORY.md`)

아래 두 문안은 **특수기호를 쓰지 않은 평문**입니다. App Store Connect 의 입력칸은
마크다운을 해석하지 않아서, 별표나 가운뎃점을 넣으면 그 글자가 그대로 보입니다.
그대로 복사해 붙이면 됩니다.

---

## 한국어 (App Store)

```
잠금화면에서 바로 꺼내 쓸 수 있습니다.

즐겨찾기 문구를 잠금화면 위젯에 두면 잠금을 풀지 않고 눌러서 복사할 수 있습니다.

Pro 를 열었습니다. 기본 기능은 그대로 무료로 쓰시고, 더 많은 칸과 잠긴 단축어 같은 것이 필요할 때만 고르시면 됩니다.
```

---

## English (App Store)

```
Copy straight from the Lock Screen.

Put a favorite snippet in the Lock Screen widget and tap it to copy without unlocking.

Pro is here. The basics stay free, and Pro is there only if you need more slots and locked snippets.
```

---

## 심사·배포 메모 (내부)

⚠️ **이 문서는 나중에 기록에서 되살린 것이다.** 그때 실제로 스토어에 올린 문안이 아니라,
버전을 올린 커밋과 그 앞의 작업 기록을 훑어 **그 버전에서 바뀐 것**을 다시 쓴 것이다.
당시 문안이 따로 있었다면 그쪽이 사실이다.

- 버전 올린 커밋: `fbb5b56` (2026-02-21)
- 무료/유료를 가른 버전이다. 이전 구매자 승계(그랜드파더링)는 4.0.1 구간에서 다듬었다
- `HISTORY.md` 의 v4.0.0 절에 같은 내용이 한 줄로 남아 있다
