# 중국어 스토어 문안 (간체 · 번체)

App Store Connect 의 **앱 정보 > 현지화 가능한 정보**에 넣을 값입니다.
zh-Hans(간체)와 zh-Hant(번체)는 **각각 따로** 등록해야 합니다.

- 이름 30자, 부제 30자. 중국어는 한 글자가 1자로 셉니다.
- ⚠️ 앱 이름은 **전역에서 유일**해야 하고, 바꾸면 **심사를 다시 받습니다.**
  1순위가 이미 쓰이고 있으면 2순위, 브랜드 자체가 막히면 3순위로 내려갑니다.
- ⚠️ 3순위로 내려가면 앱 안 표시 이름(`*/zh-Han*.lproj/InfoPlist.strings` 의
  `CFBundleDisplayName`)도 같이 바꿔야 스토어와 홈 화면이 어긋나지 않습니다.

---

## 1순위 (추천)

### 간체 zh-Hans

```
剪贴键盘 - 快捷短语一键输入
```

```
常用语·模板·剪贴板，一点即输入
```

### 번체 zh-Hant

```
剪貼鍵盤 - 快捷短語一鍵輸入
```

```
常用語·範本·剪貼簿，一點即輸入
```

이름(15자)과 부제(16자)가 **서로 다른 낱말**을 답니다. 이름은 `快捷短语`·`一键输入`,
부제는 `常用语`·`模板`·`剪贴板`. 두 칸 모두 검색 가중치가 높아서 같은 말을 두 번 쓰면
그만큼 손해입니다.

번체는 글자만 바꾸지 않았습니다. `模板`은 대만에서 `範本`, `剪贴板`은 `剪貼簿` 입니다.
앱 안 문구를 대만 어휘로 맞춘 것과 같은 규칙(`scripts/zh_hant_vocab.py`)을 따릅니다.

---

## 2순위 (1순위 이름이 막혔을 때)

| | 간체 | 번체 |
| --- | --- | --- |
| 이름 | `剪贴键盘 - 常用语快捷输入` (14자) | `剪貼鍵盤 - 常用語快捷輸入` (14자) |
| 부제 | `存好常用语，任何 App 一点就输入` (18자) | `存好常用語，任何 App 一點就輸入` (18자) |

---

## 3순위 (브랜드 `剪贴键盘` 자체가 막혔을 때)

| | 간체 | 번체 |
| --- | --- | --- |
| 이름 | `快捷短语 - 短语键盘与剪贴板` (15자) | `快捷短語 - 短語鍵盤與剪貼簿` (15자) |
| 부제 | `存好常用语，任何 App 一点就输入` (18자) | `存好常用語，任何 App 一點就輸入` (18자) |

⚠️ 이 경우 앱 안 표시 이름도 `快捷短语` / `快捷短語` 로 함께 바꿉니다.

---

## 가운뎃점을 피해야 할 때

부제의 `·` 가 마음에 걸리면 쉼표판을 씁니다. 자수는 같습니다.

| | 간체 | 번체 |
| --- | --- | --- |
| 부제 | `常用语，模板，剪贴板，一点即输入` (16자) | `常用語，範本，剪貼簿，一點即輸入` (16자) |

---

## ⚠️ 이름에 `键盘` 을 넣을 때의 위험

중국어 사용자가 `键盘` 으로 검색하면 대개 **병음 입력기(输入法)** 를 찾습니다.
이 앱은 저장해 둔 문구를 넣는 키보드지 중국어를 **타이핑하는** 입력기가 아닙니다.
오해하고 받은 사람은 곧바로 별 하나를 줍니다.

그래서 부제는 `短语`·`常用语`·`剪贴板` 처럼 **"저장해 둔 것을 꺼내 넣는다"** 쪽으로
몰아 두었습니다. 설명 첫 문단에서도 이 점을 먼저 못박는 편이 안전합니다.

---

## 설명 · 키워드 · 프로모션 텍스트

**2026-08-29 에 App Store Connect 5.0.5 의 zh-Hans · zh-Hant 로케일에 넣었다.**
(새로운 기능 문안은 `docs/release-notes/5.0.5.md` 의 간체·번체 블록이 원본이다.)

⚠️ 키워드는 **변환하지 않았다.** 대만에서 실제로 검색하는 말이 따로 있다.
   罐頭訊息·文字捷徑 은 대륙에서 안 쓰고, 剪切板·粘贴板 은 대만에서 안 쓴다.
   글자만 바꾸면 양쪽 다 헛도는 낱말이 된다.

⚠️ 이름·부제와 겹치는 낱말은 키워드에서 뺐다. 애플은 이름·부제·키워드를 함께
   훑으므로 같은 말을 두 번 넣으면 그만큼 자리를 버리는 셈이다.

### 간체 zh-Hans

설명 (617/4000자)

```
别再一遍遍打同样的话。存一次，之后点一下就输入。

把常用的账号、地址、自我介绍、邮件模板存成短语。在微信、邮件、Safari，任何能打字的地方切到剪贴键盘，点一下，内容就填进输入框。

能做什么

短语：存好的内容原样输入，一点就好。
模板：每次会变的地方留成空位，用的时候从存过的值里挑一个填上。
组合：几个值放在同一个键上，按顺序依次输入，或者用箭头轮换着用。
加密短语：护照号、卡号用 Face ID 锁起来，需要的时候才打开。
智能剪贴板：复制过的内容自动归类，邮箱、电话、地址、卡号、IBAN 都认得出来，之后一秒就能找到。
iCloud 备份：换手机也不会丢，图片短语也一起带走。

关于隐私

你写的内容只保存在你自己的设备上。开启 iCloud 备份时，会以加密状态存进你自己的 iCloud。我们不收集短语的内容，也不使用任何第三方分析工具。

请先知道这一点

这是一个用来取用存好短语的键盘，不是拼音输入法。它不替代你平时打字的键盘。需要填入常用内容时切过来点一下，填完再切回去。

免费与 Pro

免费版可以存 10 个短语、3 个模板、3 个组合、50 条剪贴板记录和 5 个图片短语。Pro 是一次性买断，不是订阅：解除所有上限，另外提供 iCloud 备份和生物识别锁，还有 7 天免费试用。

支持 iPhone、iPad、Mac 和 Vision Pro。界面提供简体中文、繁体中文、韩语、英语和俄语。
```

키워드 (52/100자)

```
短语,剪切板,粘贴板,剪贴板历史,快捷回复,文字替换,自定义键盘,复制粘贴,短语库,签名,快捷输入,效率
```

프로모션 텍스트 (66/170자)

```
有人问能不能支持中文，于是有了这一版：简体和繁体两套，还能在 App 里单独选语言。空位可以改名和删除，键盘里也能直接调整短语顺序。
```

### 번체 zh-Hant

설명 (619/4000자)

```
別再一遍遍打同樣的話。存一次，之後點一下就輸入。

把常用的帳號、地址、自我介紹、郵件範本存成短語。在LINE、郵件、Safari，任何能打字的地方切到剪貼鍵盤，點一下，內容就填進輸入框。

能做什麼

短語：存好的內容原樣輸入，一點就好。
範本：每次會變的地方留成空位，用的時候從存過的值裡挑一個填上。
組合：幾個值放在同一個鍵上，按順序依次輸入，或者用箭頭輪換著用。
加密短語：護照號、卡號用 Face ID 鎖起來，需要的時候才開啟。
智慧剪貼簿：複製過的內容自動歸類，信箱、電話、地址、卡號、IBAN 都認得出來，之後一秒就能找到。
iCloud 備份：換手機也不會丟，圖片短語也一起帶走。

關於隱私

你寫的內容只儲存在你自己的裝置上。開啓 iCloud 備份時，會以加密狀態存進你自己的 iCloud。我們不收集短語的內容，也不使用任何第三方分析工具。

請先知道這一點

這是一個用來取用存好短語的鍵盤，不是拼音輸入法。它不替代你平時打字的鍵盤。需要填入常用內容時切過來點一下，填完再切回去。

免費與 Pro

免費版可以存 10 個短語、3 個範本、3 個組合、50 條剪貼簿記錄和 5 個圖片短語。Pro 是一次性買斷，不是訂閱：解除所有上限，另外提供 iCloud 備份和生物識別鎖，還有 7 天免費試用。

支援 iPhone、iPad、Mac 和 Vision Pro。介面提供簡體中文、繁體中文、韓語、英語和俄語。
```

키워드 (48/100자)

```
短語,剪貼簿,罐頭訊息,快速回覆,文字捷徑,自訂鍵盤,複製貼上,範本,簽名檔,常用句,片語,效率
```

프로모션 텍스트 (66/170자)

```
有人問能不能支援中文，於是有了這一版：簡體和繁體兩套，還能在 App 裡單獨選語言。空位可以改名和刪除，鍵盤裡也能直接調整短語順序。
```

---

## 로케일별 링크 (개인정보 · 지원 · 마케팅)

세 링크는 저장되는 자리가 다릅니다. 개인정보만 **앱 정보**(`appInfoLocalizations`,
이름·부제와 같은 레코드)에 있고, 지원·마케팅은 **버전별 정보**
(`appStoreVersionLocalizations`, 설명·키워드와 같은 레코드)에 있습니다.

| 로케일 | 개인정보 처리방침 | 지원 · 마케팅 |
| --- | --- | --- |
| zh-Hans | `https://m1zz.github.io/ClipKeyboard/privacy.html?lang=zh-Hans` | `https://m1zz.github.io/ClipKeyboard/?lang=zh-Hans` |
| zh-Hant | `https://m1zz.github.io/ClipKeyboard/privacy.html?lang=zh-Hant` | `https://m1zz.github.io/ClipKeyboard/?lang=zh-Hant` |

2026-08-29 기준 네 개 다 5.0.5 초안에 저장돼 있고, 페이지도 살아 있습니다.

⚠️ **`?lang=` 을 반드시 붙입니다.** 파라미터가 없으면 페이지는 **보는 사람의 브라우저
   언어**를 따릅니다. 심사자의 기기가 영어면 중국어 처리방침 대신 영어가 뜹니다.
   지금 한국어 링크에는 파라미터가 없어서 같은 위험이 있습니다(`?lang=ko` 를 붙이는 편이
   확실합니다).

### 페이지는 `main` 에서 나갑니다

GitHub Pages 는 `main` 의 `docs/` 만 내보냅니다. 중국어를 `dev` 에서 아무리 고쳐도
링크는 영어로 떨어집니다. 2026-08-29 에 웹페이지 다섯 개와 아이콘 두 개를 `main` 에
올려서(커밋 9a89632) 살렸습니다. **앞으로 페이지 문구를 고치면 `main` 에도 올려야
스토어 링크에 반영됩니다.**

다섯 페이지 모두 한국어·영어·간체·번체가 빠짐없이 채워져 있는 것을 확인했습니다
(랜딩 65키 · 가이드 157키 · 개인정보 90키 · 약관 43키, 손쉬운사용은 언어별 블록 54개씩).

### 영어 링크: 살아 있는 쪽은 아직 남의 사이트입니다

`appInfoLocalizations` 는 두 벌입니다. 판매 중인 쪽(READY_FOR_SALE)의 en-US 개인정보
URL 이 `https://codershigh.github.io/WebSite/privacypolicy.html` 인데, 열어 보면
**이 앱과 무관한 코딩 학원의 2018년 방침**이고 한국어입니다. 지금 영어 스토어에서
그게 보입니다.

5.0.5 초안 쪽은 이미 `.../privacy.html?lang=en` 으로 고쳐져 있어서, **5.0.5 가 나가면
저절로 정리됩니다.** 그 전에 손보고 싶으면 판매 중인 레코드를 직접 고쳐야 합니다.

---

## 아직 안 된 것 (같은 화면에서 함께 채울 것)

이름·부제만으로는 페이지가 서지 않습니다. zh-Hans·zh-Hant 각각에 다음이 더 필요합니다.

- ~~설명 · 프로모션 텍스트 · 키워드~~ 넣었습니다(위 항목).
- ~~5.0.5 "새로운 기능" 중국어판~~ 넣었습니다.
- ~~앱 이름과 부제~~ 넣었습니다. 번체 1순위(`剪貼鍵盤`)는 이미 쓰이는 이름이라 거절돼서,
  간체·번체 모두 `ClipKeyboard - 快捷短语一键输入` / `ClipKeyboard - 快捷短語一鍵輸入` 로 갔습니다.
- ~~개인정보 · 지원 · 마케팅 URL~~ 넣었습니다(위 항목).
- **스크린샷** (로케일별로 따로 올립니다. 지금은 ko·en 것만 있습니다)
