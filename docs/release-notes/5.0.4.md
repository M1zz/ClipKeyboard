# 5.0.4 릴리즈 노트

앱 스토어 "이번 버전의 새로운 기능"에 그대로 붙여 넣을 수 있는 문안입니다.
(앱 안 변경 이력은 `ChangelogView.swift` 의 `5.0.4` 항목, 저장소 기록은 `HISTORY.md`)

아래 두 문안은 **특수기호를 쓰지 않은 평문**입니다. App Store Connect 의 입력칸은
마크다운을 해석하지 않아서, 별표나 가운뎃점을 넣으면 그 글자가 그대로 보입니다.
그대로 복사해 붙이면 됩니다.

---

## 한국어 (App Store)

```
아낀 시간을 다른 것에 빗대어 보여줍니다.

친구들에게 알리기로 만드는 영상이 바뀌었습니다. 예전에는 아낀 시간이 그대로 큰 글씨였는데, 시간은 크기가 잡히지 않는 단위입니다. 한 시간 반을 아꼈다는 말을 읽고 그게 큰지 작은지 알려면 머릿속에서 한 번 더 환산해야 하고, 남의 스토리에서 넘겨 보는 3초 안에 그 환산은 일어나지 않습니다.

이제 큰 자리에는 환산이 끝난 것이 섭니다. 12.5km 달릴 수 있는 시간, 드라마 여덟 편, 책 세 권, 마라톤 네 번, 최저임금으로 쳐도 커피 열두 잔. 열 가지 중에서 열 때마다 다른 것이 뽑히고, 마음에 안 들면 다른 걸로 버튼으로 굴려 볼 수 있습니다.

돈으로 셈하는 것은 사는 나라의 최저임금과 물건값으로 합니다. 금액을 그대로 적지 않고 커피 잔 수로 바꾸는 데는 이유가 있습니다. 금액은 해마다 조금씩 틀려지지만 잔 수는 오래 버팁니다. 최저임금과 커피값이 대체로 같이 오르기 때문입니다. 사는 곳의 값을 모르면 돈으로 셈하는 것은 아예 나오지 않고 다른 것이 나옵니다.

아낀 시간과 실제 횟수는 사라지지 않고 아래에 작게 남습니다. 큰 글씨는 어림한 것이고 그 줄만 실제로 센 것이라, 어림한 숫자만 있는 그림은 자랑이 아니라 광고가 됩니다.

설정을 손봤습니다.

배경 이미지를 설정에서도 직접 넣고 지울 수 있습니다. 내 사진으로 배경을 깔고 지우는 길이 그동안 단축어 목록 화면의 선택기에만 있었습니다. 배경을 바꾸러 설정에 들어온 분은 내장된 여덟 장만 보고 이게 전부인 줄 아셨을 겁니다. 같은 이름의 화면이 둘인데 할 수 있는 일이 다르면, 사람은 자기가 본 쪽을 그 기능의 전부로 압니다. 지우기는 한 번 되묻고, 사진첩의 원본은 건드리지 않습니다.

키보드 연습하기를 뺐습니다. 바로 위의 키보드 설정 가이드가 같은 일을 하고 있었습니다.

붙여넣기 알림 설정은 붙여넣기 알림 허용 끄기로 이름을 바꿨습니다. 설정이라고만 적혀 있으면 들어가 봐야 무엇을 할 수 있는지 알 수 있습니다. 하려는 일을 적어 두면 들어가기 전에 압니다.

빈칸 관리, 카테고리 관리, 보관함을 나란히 모았습니다. 셋 다 만들어 둔 것을 들여다보는 자리인데 보관함만 위쪽 페르소나 옆에 떨어져 있었습니다.

페르소나를 언제 여쭤볼지 바꿨습니다.

처음 여는 자리에서 여쭤봤습니다. 그 자리에서는 아무도 답을 모릅니다. 앱을 아직 안 써 보셨으니 디지털 노마드와 학생 중에 무엇을 고르면 자기 화면이 어떻게 되는지 알 길이 없고, 무엇보다 아직 이 앱이 무엇인지도 모르십니다.

이제 써 보고 나서 여쭤봅니다. 단축어를 두 개 만드셨거나 카테고리를 하나 만드셨다면, 그때는 이 앱으로 무엇을 하시려는지 자신이 아십니다. 그 자리에서 혹시 이런 분이신가요 하고 한 번만 여쭙니다. 나중에 할게요로 넘기셔도 됩니다. 답을 하셨든 넘기셨든 다시 여쭙지 않습니다.

그전까지는 일반으로 둡니다. 예전 기본값은 디지털 노마드였습니다. 그건 이 앱이 국제 송금과 비자에서 출발했다는 만든 사람의 사정이지 쓰시는 분의 사정이 아닙니다. 처음 여는 분 대부분은 전화번호와 주소를 넣으려고 오셨는데, 그분께 IBAN과 여권번호를 들이밀면 이 앱이 자기 것이 아닌 줄 아십니다.

사용 기록 화면의 카드 두 장을 한 장으로 합쳤습니다.

세 시간을 벌었다는 축하 카드와, 그 아래 다시 치지 않은 횟수 카드가 위아래로 나란히 서서 같은 말을 두 번 하고 있었습니다. 하나는 세 시간을 벌었어요, 하나는 대략 3시간을 아꼈어요. 이제 한 장입니다. 축하할 일이 있는 날에는 그 카드가 물듭니다.

빗대는 줄은 눌러서 바꿉니다. 영화 한 편이 안 와닿으면 눌러 보세요. 30km 달릴 수 있는 시간, 드라마 네 편, 책 135쪽, 최저임금으로 쳐도 커피 일곱 잔으로 바뀝니다. 크기를 잡아 주려고 둔 줄인데 한 가지 자로만 재면 그 자를 모르는 사람은 여전히 크기를 못 잡습니다.

체크 도장은 언제나 연두입니다. 축하 카드의 도장만 혼자 키 컬러를 따라가고 있었습니다.

키보드에서 어떤 키는 눌러도 아무 일이 없었습니다. 그 키가 고장 난 것이 아니라, 옆에 있던 이미지 단축어가 그 위를 덮고 있었습니다.

가로로 긴 사진을 담은 단축어는 자기 칸보다 넓게 누워 있었습니다. 사진이 칸을 채울 때까지 커지는데 폭을 붙잡아 두는 것이 없어서, 넘친 만큼이 옆 칸으로 흘러 들어갔습니다. 눈에는 사진이 반듯하게 잘려 보였습니다. 자르는 것은 그림만 자르고 손가락이 닿는 자리는 그대로 두기 때문입니다. 그래서 옆 키를 눌러도 손가락은 사진에 닿고 있었습니다.

파노라마나 잘라낸 화면처럼 아주 긴 사진이면 옆 키가 통째로 가려졌습니다. 영수증이나 명함처럼 조금 긴 사진이면 옆 키의 한쪽 끝만 안 눌렸습니다. 사진이 세로거나 정사각이면 아무 일도 없었습니다. 키를 몇 줄로 놓았는지, 키 높이를 얼마로 두었는지에 따라서도 갈렸습니다. 어떤 분에게는 늘 그랬고 어떤 분에게는 한 번도 없었던 이유가 이것입니다.

이제 사진이 아무리 길어도 키는 자기 칸 안에 머뭅니다. 옆 키는 옆 키가 됩니다.

이미지 단축어의 둥근 모서리도 이제 눌립니다. 예전에는 네 귀퉁이가 손가락을 받지 않는 죽은 자리였습니다.
```

---

## English (App Store)

```
Your saved time, said in something you can picture.

The video you share has changed. It used to put the saved time itself in the big type, but time is a unit you cannot size up. Reading that you saved an hour and a half tells you nothing about whether that is a lot until you convert it in your head, and nobody does that in the three seconds a story gets.

Now the big type holds the conversion already done. Far enough to run 12.5km. Eight episodes. Three books. Four marathons. Twelve cups of coffee, even at minimum wage. Ten of them, a different one each time you open it, and a Try another button if you want to roll again.

The ones counted in money use the minimum wage and prices where you live. There is a reason we say cups instead of an amount: an amount drifts wrong a little every year, and a cup count holds far longer, because minimum wage and the price of coffee move together. If we do not have prices for where you are, the money ones simply do not appear and you get something else.

The saved time and the real count stay, small, underneath. The big number is an estimate and that line is the part we actually counted. A picture with nothing but estimates on it is not a brag, it is an ad.

Settings got a pass.

You can now add and delete your own background image from Settings. Putting your own photo behind the list, and removing it, only existed in the picker on the snippet list screen. Anyone who went to Settings to change the background saw the eight built in ones and assumed that was all there was. When two screens share a name but not their abilities, people take whichever one they found as the whole feature. Deleting asks once, and the original in your photo library is left alone.

Keyboard practice is gone. The keyboard setup guide directly above it was doing the same job.

Paste notification settings is now Turn off the paste alert. A row that only says settings makes you open it to find out what it can do. Naming the thing you want to do tells you before you go in.

Placeholders, categories and the inbox now sit together. All three are places to look at what you have made, but the inbox was off on its own next to persona.

We changed when we ask about your persona.

We used to ask on the very first screen. Nobody knows the answer there. You have not used the app yet, so there is no way to know what picking Digital Nomad over Student does to your screen, and more to the point you do not yet know what this app is.

Now we ask after you have used it. Once you have made two snippets or created a category, you know what you came here to do. That is when we ask, once, whether this sounds like you. You can skip it. Answered or skipped, we do not ask again.

Until then it is set to General. The old default was Digital Nomad. That was a fact about where this app started, international transfers and visas, not a fact about you. Most people opening it for the first time came to store a phone number and an address, and showing them IBAN and passport number tells them this app is not for them.

Two cards on the usage screen became one.

A card saying you had earned three hours sat right above a card saying you had saved about three hours. The same sentence, twice, one under the other. It is one card now, and it takes on color on a day worth celebrating.

The comparison line changes when you tap it. If a movie does not land for you, tap. Far enough to run 30km. Four episodes. 135 pages. Seven cups of coffee, even at minimum wage. A line meant to give you a sense of scale fails at that job if it only ever measures with one ruler.

The check seal is always yellow green. The one on the old celebration card was the only place in the app still following the key color.

Some keys on the keyboard did nothing when tapped. The key was not broken. An image snippet beside it was lying on top of it.

A snippet holding a wide photo was laid out wider than its own cell. The photo grows until it covers the cell, and nothing was holding its width, so the overflow spilled into the cell next door. On screen the photo still looked neatly cropped, because cropping trims the picture and leaves the touch area where it was. Tapping the key beside it meant putting your finger on the photo.

With a very wide photo, a panorama or a cropped screenshot, the neighboring key was covered entirely. With a mildly wide one, a receipt or a business card, only one edge of it went dead. A portrait or square photo did nothing at all. How many columns of keys you use and how tall you set them changed it too. That is why this happened constantly to some people and never to others.

Now the key stays inside its own cell no matter how long the photo is. The key next door is the key next door again.

The rounded corners of an image snippet key now respond as well. They used to be dead spots that would not take a finger.
```

---

## 심사·배포 메모 (내부)

- 버전: `Version.xcconfig` 의 `MARKETING_VERSION = 5.0.4`, `CURRENT_PROJECT_VERSION = 1`.
  App Store Connect 에 5.0.4(1) 이 이미 올라가 있다면 빌드 번호만 올릴 것.
- **새 권한 없음. 엔타이틀먼트 변경 없음. 서버로 나가는 것 없음.**
- 새 파일: `ClipKeyboard/Service/TimeEquivalent.swift`, `ClipKeyboard/Service/PersonaPrompt.swift` (둘 다 앱 타겟에만).
  공유 영상과 사용 기록 카드가 **이 한 곳**에서 빗댈 것을 가져간다.
- 지운 파일: `ClipKeyboard/Screens/KeyboardPracticeView.swift`.
- 고친 파일: `ClipKeyboardExtension/KeyboardOverlays.swift`(`ImageMemoButton`),
  `ClipKeyboard/Service/ShareVideoRenderer.swift`, `ClipKeyboard/Screens/ShareVideoSheet.swift`,
  `ClipKeyboard/Screens/UsagePassportView.swift`, `ClipKeyboard/Screens/SettingView.swift`,
  `ClipKeyboard/Screens/ListBackgroundSettings.swift`, `ClipKeyboard/Screens/SnippetsTab.swift`,
  `ClipKeyboard/Screens/KeyboardSetupOnboardingView.swift`.

### 이번에 더한 것

공유 영상의 큰 자리를 시간에서 **바꿔 말하기**로 옮겼다. 갈래 열 개(달리기·마라톤·
걷기·드라마·노래·책 쪽·책 권·지하철·커피·라면)를 두고, 아낀 시간에 **말이 되는 것만**
추려 그중 하나를 뽑는다. 표와 근거는 `docs/engineering/SHARE_VIDEO_EQUIVALENTS.md`.

각 갈래에 아래 문턱과 **위 천장**이 함께 있다. 천장이 없던 첫 판에서는 오래 쓴 사람에게
"라면 1,218봉지"·"지하철 4,250정거장"이 나왔다. 그려지지 않는 숫자는 크기를 전달하지
못할 뿐 아니라, 아래 작은 줄의 진짜 횟수까지 같이 못 믿게 만든다.

돈으로 셈하는 갈래는 `LocalPrices` 의 나라별 표(최저시급·커피값·라면값)를 쓴다.
**금액을 적지 않고 물건 개수로 바꾼 것이 핵심이다.** 우리가 쓰는 것은 두 값의 비율이고
비율은 물가를 타지 않아서, 표가 몇 해 낡아도 잔 수는 그대로다. 그래도 `asOf` 로부터
3년이 지나거나 지역이 표에 없으면 **그 갈래는 통째로 빠진다.**

### 설정 정리

- `KeyboardPracticeView.swift` **삭제**. 설정의 유일한 진입점을 빼면서 파일 전체가 고아가
  됐다(`KeyboardPracticeSheet` 도 아무도 안 불렀다). pbxproj 항목도 같이 걷었다.
- "붙여넣기 알림 설정" → **"붙여넣기 알림 허용 끄기"** (행 라벨·화면 제목 둘 다).
- 행 순서: 빈칸 관리 · 카테고리 관리 · 보관함을 **붙여 놓았다.** 보관함이 페르소나 옆에
  있었다. 고르는 것들 사이에 담아 두는 것이 하나 끼어 있던 꼴이다.
- `ListBackgroundSettings` 에 **내 사진 넣기·지우기**를 넣었다. `BackgroundImageStore` 는
  이미 다 갖추고 있었고 목록 화면 선택기만 그걸 쓰고 있었다. 썸네일도
  `Image(name)` → `BackgroundImageView(name:)` 로 바꿨다. 안 바꾸면 내 사진이 **빈 칸**으로
  나온다(이름만 맞고 에셋이 없다).
  지우기는 되묻고, 탭별 덮어쓰기에 남은 같은 이름도 함께 비운다. 안 비우면 **어떤 탭에서만**
  배경이 사라진 채로 남는다.

### 페르소나를 언제 묻는가

`Persona.default` 를 `.nomad` → `.general` 로. 그리고 새 파일 `PersonaPrompt.swift` 가
**언제 물을지**를 혼자 정한다.

    tutorialDone && !wasAsked && selectedPersona == nil
      && (snippetCount >= 2 || customCategoryCount >= 1)

⚠️ 튜토리얼 중에는 **절대 안 묻는다.** 안내 도중에 다른 판이 끼어들면 지금 무엇을 하던
중이었는지 놓친다. 이미 떠 있는 판(무대 권유·샘플 치우기)이 있으면 비켜 있다가 다음
기회에 다시 본다.

⚠️ 카테고리는 자동 시드되지 않는다(신규 설치는 `categories = []`). 그래서 하나라도 있으면
**사용자가 직접 만든 것**이다. 시드가 있었다면 이 조건은 모두에게 즉시 참이 됐을 것이다.

⚠️ 전체 화면이 아니라 **시트**로 띄운다. 부른 적 없는 질문이라 하던 일을 끊을 자격이 없다.
쓸어내려 닫는 것도 "나중에" 와 같게 친다(`onDismiss` 에서 `markAsked`).

⚠️ 오래 쓴 사람 중 페르소나를 한 번도 안 고른 사람에게는 **다음 실행에 한 번 뜬다.**
의도한 것이다. 그 사람은 물어본 적이 없었다.

### 카드 둘을 하나로

`UsagePassportView` 의 축하 카드(`celebration`)를 없애고 머리말 카드가 그 일을 겸하게 했다.
둘로 나눠 두었더니 "세 시간을 벌었어요" 와 "대략 3시간을 아꼈어요" 가 위아래로 나란히
섰다. 이정표에 새로 닿은 날에는 머리말 카드 바탕이 `accentSoft` 로 물들고, 이정표가
데려온 문구가 빗대는 줄의 **첫 칸**에 들어간다.

빗대는 줄은 눌러서 넘긴다. 목록은 `TimeEquivalentCatalog` 가 만든다(공유 영상과 **같은
출처**다). 무작위가 아니라 **순서대로** 넘어간다 - 무작위로 뽑으면 눌렀는데 같은 것이
다시 나오고, 그건 안 눌린 것과 구별되지 않는다.

⚠️ 영상의 `caption` 과 화면의 `localizedSentence` 는 **다른 문자열이다.** 앞의 것은 큰
글씨 아래에 떨어져 설 때 읽히도록 쓴 것이라 이어 붙이면 말이 안 된다
("12잔 최저임금으로 쳐도 커피 값이에요").

체크 도장은 `Color.checkGreen` 으로 고정. 옛 축하 카드의 도장만 혼자 `theme.accent` 를
따라가고 있었다.

### 고친 것

`ImageMemoButton` 이 `ZStack` 안에서 사진을 이렇게 그리고 있었다.

```swift
Image(uiImage: image)
    .resizable()
    .scaledToFill()
    .frame(height: buttonHeight)   // 높이만 묶고 폭은 안 묶었다
    .clipShape(...)
```

`scaledToFill` 은 제안받은 칸을 **덮을 때까지** 키운다. 폭을 붙잡는 것이 없으니
`aspectRatio(.fill)` 이 돌려주는 폭이 그대로 `ZStack` 의 폭이 되고, `LazyVGrid` 셀을
넘어선다. `clipShape` 는 **그림만** 자르고 배치와 히트 테스트는 건드리지 않는다.
`LazyVGrid` 에서는 뒤에 오는 항목이 위에 그려지므로, 넘친 이미지 키가 **자기 왼쪽 키**의
탭을 가져간다.

SwiftUI 레이아웃을 그대로 태워 실측한 값(셀 폭 65.8pt, 키 높이 44pt):

| 사진 | 고치기 전 배치 폭 | 넘침 |
| --- | --- | --- |
| 정사각 1024x1024 | 65.8 | 0 |
| 가로 1024x768 (4:3) | 65.8 | 0 |
| 와이드 1024x400 (2.56:1) | 112.6 | +46.8 |
| 파노라마 1024x200 (5:1) | 225.3 | +159.5 |

사진 가로세로비가 `셀 폭 / 키 높이` 보다 크면 터진다. 3열이면 2.6:1, 5열이면 1.5:1 이
경계라 **열이 많거나 키 높이를 낮게 둔 사람일수록 잘 걸린다.**

고침은 사진을 `ZStack` 자식에서 `overlay` 로 옮긴 것이다. `overlay` 의 자식은 부모 크기를
제안받고 **자기 크기는 부모 배치에 영향을 주지 않는다.** 같은 실측에서 네 경우 모두
셀 폭에 정확히 맞는다. 여기에 `contentShape(Rectangle())` 을 붙여 둥근 귀퉁이까지
누를 자리로 살렸다.

### ⚠️ 확인 (설정·페르소나)

1. 설정 > 배경 이미지에서 **내 사진**으로 한 장 넣고 곧바로 적용되는지 본다.
2. 그 사진의 X 를 눌러 되묻는지, 지운 뒤 배경이 "없음" 으로 돌아가는지 본다.
   지금 쓰고 있지 않은 사진을 지웠을 때는 현재 배경이 그대로여야 한다.
3. 목록 화면 선택기에서 넣은 사진이 **설정 화면에도 보이는지** 본다(같은 저장소다).
4. 설정 > 단축어 섹션에 "키보드 연습하기" 가 없고, 붙여넣기 행이
   "붙여넣기 알림 허용 끄기" 로 적혀 있는지 본다.
5. 새 설치로 튜토리얼을 끝내고 단축어를 **두 개** 만들어 "혹시 이런 분이신가요?" 가
   뜨는지 본다. 하나만 만들었을 때는 안 떠야 한다.
6. "나중에 할게요" 로 닫고 앱을 다시 켜 **다시 안 뜨는지** 본다. 쓸어내려 닫았을 때도 같다.
7. 튜토리얼 도중에 단축어가 여러 개 있어도 안 뜨는지 본다.

### ⚠️ 확인 (사용 기록 카드)

1. 빗대는 줄을 **여러 번 눌러** 끝까지 돌고 처음으로 되돌아오는지 본다.
2. 위 기간 고르개를 바꿨을 때 줄이 첫 칸으로 돌아가는지 본다(목록 자체가 달라진다).
3. 이정표에 새로 닿은 상태를 만들어(설정에서 튜토리얼 초기화 또는
   `SavedTimeMilestone.resetAll()`) 카드가 물들고 그 문구가 첫 칸인지 본다.
4. 아주 적게 쓴 계정에서 빗대는 줄이 아예 안 나오는지 본다(빈 줄이 남으면 안 된다).
5. 보이스오버로 그 줄에 서서 "탭하면 다른 것에 빗대어 보여줍니다" 가 읽히는지 본다.

### ⚠️ 실기기 확인 (공유 영상)

1. 사용 기록에서 **친구들에게 알리기**를 열고 미리보기가 도는지 본다.
2. **다른 걸로**를 여러 번 눌러 갈래가 실제로 바뀌는지, 연달아 같은 것이 안 나오는지 본다.
3. 라이트·다크 양쪽에서 한 번씩 굽는다. 큰 글씨가 잘리지 않는지(마라톤·책 권은 이름까지
   들어가서 가장 길다) 본다.
4. 기기 지역을 표에 **없는 곳**(예: 브라질)으로 바꾸고 커피·라면이 안 나오는지 본다.
5. 아주 적게 쓴 계정에서 열어 시간이 큰 자리로 되돌아가는지 본다.

### ⚠️ 실기기 확인 (이미지 키)

시뮬레이터로도 재현되지만, 손가락 크기가 걸린 문제라 아이폰에서 볼 것.

1. **가로로 아주 긴 사진**(파노라마·잘라낸 화면)을 이미지 단축어로 만든다.
2. 설정에서 **키 열 수를 5로, 키 높이를 낮게** 둔다. 가장 잘 터지던 조합이다.
3. 그 이미지 키 **왼쪽 키**를 누른다. 고치기 전에는 반응이 없었다.
4. 이미지 키의 **네 귀퉁이**를 눌러 값이 복사되는지 본다.
5. 세로 사진·정사각 사진도 예전처럼 칸을 꽉 채워 보이는지 함께 본다
   (`overlay` 로 옮기면서 채우는 방식이 바뀌지 않았는지).

### 남겨 둔 것 (다음 판)

- `ImageMemoButton` 은 앱 목록(`ClipKeyboardListComponents`)과 달리 **썸네일 캐시도
  다운샘플링도 없이** 원본을 `onAppear` 마다 로드한다. 메모리가 빠듯한 키보드
  익스텐션이라 이미지 단축어가 많으면 위험하다.
- `ClipKeyboard/Screens/List/ClipKeyboardList.swift:1161,1188` 의
  `.frame(height:150).frame(maxWidth:.infinity)` 도 같은 갈래의 잠재 오버플로우다.
  단일 열이라 증상이 드러나지 않을 뿐이다.
