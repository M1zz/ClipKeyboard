# 4.2.2 릴리즈 노트

앱 스토어 "이번 버전의 새로운 기능"에 그대로 붙여 넣을 수 있는 문안입니다.
(앱 안 변경 이력은 `ChangelogView.swift` 의 `4.2.2` 항목, 저장소 기록은 `HISTORY.md`)

아래 두 문안은 **특수기호를 쓰지 않은 평문**입니다. App Store Connect 의 입력칸은
마크다운을 해석하지 않아서, 별표나 가운뎃점을 넣으면 그 글자가 그대로 보입니다.
그대로 복사해 붙이면 됩니다.

---

## 한국어 (App Store)

```
갈래를 하나로 정리했습니다.

갈래를 만들고 고치는 자리가 여기저기 나뉘어 있던 것을 한곳으로 모았습니다. 키보드에서 갈래 색이 엉뚱하게 나오던 것도 고쳤습니다.

즐겨찾기 색을 직접 고를 수 있습니다.
```

---

## English (App Store)

```
Categories are one thing now.

Making and editing categories was spread across several places, and it is one screen now. Category colors showing up wrong in the keyboard is fixed.

You can pick your own color for favorites.
```

---

## 심사·배포 메모 (내부)

⚠️ **이 문서는 나중에 기록에서 되살린 것이다.** 그때 실제로 스토어에 올린 문안이 아니라,
버전을 올린 커밋과 그 앞의 작업 기록을 훑어 **그 버전에서 바뀐 것**을 다시 쓴 것이다.
당시 문안이 따로 있었다면 그쪽이 사실이다.

- 버전 올린 커밋: `8dc6c06` (2026-05-31)
- 4.2.1 문안은 이 구간의 커밋에서 따로 작성됐다(`4.2.1.md`)
- 4.2.2 는 마케팅 버전을 맞추려고 올린 번호에 가깝다
